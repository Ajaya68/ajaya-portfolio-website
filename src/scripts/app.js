import React, { useEffect } from 'react';

function App() {
  useEffect(() => {
    // Static HTML portfolio lives in /public/index.html.
    // React mounts into #root which is hidden; no UI rendered here.
  }, []);
  return null;
}

export default App;
